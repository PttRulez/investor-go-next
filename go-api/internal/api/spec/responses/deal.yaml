components:
  schemas:
    DealResponse:
      allOf:
        - $ref: ../requests/deal.yaml#/components/schemas/CreateDealRequest
        - type: object
          properties:
            id:
              type: integer
