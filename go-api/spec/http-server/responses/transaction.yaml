components:
  schemas:
    TransactionResponse:
      allOf:
        - $ref: ../requests/transaction.yaml#/components/schemas/CreateTransactionRequest
        - type: object
          required: [id]
          properties:
            id:
              type: integer
